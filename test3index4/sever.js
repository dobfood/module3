const http = require('http');
const fs = require('fs');
const qs = require('qs');
const formidable = require('formidable');
const sever = http.createServer(function (req, res){
    if(req.url =="./images" && method.toLowerCase()==="POST")
})
